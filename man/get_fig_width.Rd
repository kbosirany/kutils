% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fig_width.R
\name{get_fig_width}
\alias{get_fig_width}
\title{Calculate Figure Width Based on Output Width}
\usage{
get_fig_width(out.width = knitr::opts_chunk$get("out.width"))
}
\arguments{
\item{out.width}{Character string or numeric value representing the output
width percentage. Defaults to the current knitr chunk's \code{out.width} option.
Can be specified as a percentage (e.g., "70\%") or numeric value.}
}
\value{
A numeric value representing the calculated figure width in inches.
}
\description{
Calculates the actual figure width in inches based on the percentage of
output width specified in knitr chunk options. The calculation assumes a
base width of 6 inches at 70\% output width and scales proportionally.
}
\details{
The function uses the formula: \code{6 * (parsed_number / 70)} to scale the
figure width. For example:
\itemize{
\item At 70\% output width, returns 6 inches
\item At 100\% output width, returns approximately 8.57 inches
\item At 50\% output width, returns approximately 4.29 inches
}
}
\examples{
\dontrun{
# Using default chunk option
get_fig_width()

# Specifying custom width
get_fig_width("50\%")
get_fig_width(100)
}

}
